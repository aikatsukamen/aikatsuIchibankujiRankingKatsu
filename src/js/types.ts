export type RankList = {
  date: string;
  all: { name: string; rank: number }[];
  cute: { name: string; rank: number }[];
  cool: { name: string; rank: number }[];
  sexy: { name: string; rank: number }[];
  pop: { name: string; rank: number }[];
};
